# Minimal production deps for Render (Python 3.11).
# torch is installed separately in bin/render-build.sh (CPU wheel).
# Do not add langchain/langgraph/neo4j-graphrag — not used by the app.

# API
fastapi==0.135.2
uvicorn==0.42.0
starlette==1.0.0
python-multipart==0.0.32
python-dotenv==1.2.2

# Auth & database
motor==3.7.1
pymongo==4.16.0
bcrypt==5.0.0
python-jose==3.5.0
pydantic==2.12.5
email-validator>=2.0.0

# AI provider
azure-ai-inference==1.0.0b9
azure-core==1.39.0

# Embeddings & legacy vector store (torch installed before this file)
sentence-transformers==5.3.0
faiss-cpu==1.13.2

# Document parsing
pypdf==6.13.2
python-docx==1.1.2
python-pptx==1.0.2
pillow==11.3.0

# HTTP / utilities
requests==2.33.1

# Neo4j driver only (GraphRAG disabled on Render via NEO4J_ENABLED=false)
neo4j>=5.25.0,<7.0.0
